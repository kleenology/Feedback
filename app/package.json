{
  "name": "kleenology-app",
  "version": "1.0.0",
  "private": true,
  "description": "تطبيق كلينولوجي لطلب خدمات التنظيف — غلاف iOS عبر Capacitor",
  "scripts": {
    "build": "node build.mjs",
    "ios:add": "npm run build && npx cap add ios",
    "ios": "npm run build && npx cap sync ios && npx cap open ios",
    "sync": "npm run build && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor-firebase/authentication": "^6.1.0",
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
