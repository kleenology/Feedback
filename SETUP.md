# إعداد الحماية — خطوات مطلوبة منك في Firebase Console

بعد هذا التحديث، لوحة التحكم صارت تعتمد على تسجيل دخول حقيقي (Firebase Authentication) بدل رقم الـ PIN، وقاعدة البيانات تحتاج نشر قواعد حماية جديدة. **بدون هذه الخطوات لن تقدر تدخل لوحة التحكم**، فنفّذها بالترتيب:

## 1) تفعيل تسجيل الدخول بالبريد الإلكتروني

1. افتح [Firebase Console](https://console.firebase.google.com/) واختر مشروع `kleenology-d23a3`.
2. من القائمة الجانبية: **Build → Authentication**.
3. اضغط **Get started** (إن ظهرت)، ثم تبويب **Sign-in method**.
4. اختر **Email/Password** وفعّله (Enable) واحفظ.

## 2) إنشاء حساب الأدمن

1. في نفس صفحة Authentication، افتح تبويب **Users**.
2. اضغط **Add user**.
3. أدخل بريدك الإلكتروني وكلمة مرور قوية (12 حرف أو أكثر).
4. هذا هو الحساب الذي ستدخل به للوحة التحكم (اضغط على شعار ✨ ثلاث مرات كالعادة، ثم أدخل البريد وكلمة المرور).

## 3) نشر قواعد Firestore

1. من القائمة الجانبية: **Build → Firestore Database**.
2. افتح تبويب **Rules**.
3. احذف القواعد الحالية والصق محتوى ملف [`firestore.rules`](./firestore.rules) الموجود في هذا المستودع كاملاً.
4. اضغط **Publish**.

## ماذا تفعل هذه القواعد؟

- **الزائر (العميل):** يقدر فقط يرسل تقييماً جديداً، وبشرط أن تكون بياناته بالصيغة الصحيحة (تقييمات من 1 إلى 5، نصوص بحدود معقولة، كوبون غير مستخدم). لا يقدر يقرأ تقييمات غيره ولا يعدّل ولا يحذف شيئاً.
- **الأدمن (أنت):** بعد تسجيل الدخول تقدر تقرأ كل التقييمات، وتعلّم الكوبونات كمستخدمة، وتحذف البيانات.

## تحديث: صفحة عرض التقييمات والإشعارات

أضيفت مجموعة جديدة `public_reviews` لصفحة العرض العامة (`reviews.html`). عند تحديث القواعد أعد نسخ محتوى `firestore.rules` كاملاً والصقه في **Firestore → Rules → Publish** (يشمل الآن قسم `public_reviews`).

- **النشر:** من لوحة التحكم → تبويب التقييمات → زر «نشر في صفحة العرض» على أي تقييم. الاسم والتقييم والملاحظة فقط هي التي تنشر — رقم الجوال والكوبون لا ينشران أبداً.
- **الإشعارات:** لوحة التحكم صارت حية — أي تقييم جديد يصل وأنت فاتح اللوحة يظهر تنبيه فوري وتتحدث الأرقام تلقائياً. إشعارات الجوال والتطبيق مغلق تتطلب خطة مدفوعة (Cloud Functions) ولذلك لم تنفذ.

## تحديث: متابعة التقييمات السلبية وتقييم الفرق

- أي تقييم بنجمة أو نجمتين يظهر بعلامة «🚨 يحتاج متابعة» مع زر واتساب للتواصل مع العميل وزر «تمت المعالجة».
- سؤال «من الفريق اللي خدمك؟» يظهر في النموذج تلقائياً عند تعبئة أسماء الفرق في المتغير `TEAMS` أعلى السكربت في `index.html`، ويظهر تحليل «أداء الفرق» في لوحة التحكم.
- حقل الفريق (`tm`) أضيف إلى قواعد `firestore.rules` — أعد نشر القواعد كاملة عند التحديث.

## تحديث: التقارير الشهرية وأهداف الفرق

- تفاصيل الفريق فيها اختيار الشهر، ومقارنة تلقائية بالشهر السابق، وزر «🎯 تحديد هدف شهري» (هدف للتقييم وهدف اختياري لعدد التقييمات).
- الأهداف تُحفظ في مجموعة `settings` (وثيقة `targets`) للأدمن فقط، مع نسخة احتياطية على الجهاز نفسه حتى قبل نشر القواعد.
- أعد نشر `firestore.rules` كاملاً بعد هذا التحديث ليعمل حفظ الأهداف على كل الأجهزة.

## تحديث: تبويب «📣 العروض» — قائمة الأرقام والرسائل السريعة

هذا التبويب مخصص لقائمة أرقام العملاء اللي تحدّثها يومياً، ولإرسال رسائل العروض بأسرع طريقة ممكنة.

**قبل أي شيء:** أعد نسخ محتوى `firestore.rules` كاملاً والصقه في **Firestore → Rules → Publish** — أضيفت مجموعة جديدة اسمها `contacts` مقفلة على الأدمن فقط (ما يقدر أي زائر يقرأ أرقام عملائك).

### 1) إضافة القائمة اليومية

- **لصق مباشر:** انسخ العمود من الإكسل أو الأرقام من الجوال والصقها في المربع. رقم في كل سطر، والاسم قبل الرقم أو بعده (`0501234567 أبو سعد` أو `منال, 0559876543`) — كلاهما يشتغل، والفواصل والشرط والأقواس والصيغة الدولية `+966` كلها مفهومة.
- **من ملف:** زر «📄 من ملف» يقبل `CSV` أو `TXT` (صدّر من الإكسل: File → Save As → CSV UTF-8).
- **من التقييمات:** زر «👥 أضف أرقام عملاء التقييمات» يسحب أرقام كل من قيّم الخدمة.

كل رقم يُحفظ مرة واحدة فقط بصيغة موحدة (`9665xxxxxxxx`)، فتقدر تلصق قائمة اليوم كاملة كل صباح — الموجود يتجاهَل والجديد فقط ينضاف. الأرقام غير الصحيحة (أرضي، ناقصة، أسطر بدون رقم) تُرفض ويظهر لك عددها.

### 1.1) مصدر الأرقام: عميل أم ليد؟

قبل ما تضغط «أضف للقائمة» اختر **مصدر الدفعة**، لأن اللي طلب خدمة فعلاً غير اللي جاك من إعلان وما جرّبك بعد:

| المصدر | من هم | رسالتهم | يُحسبون عملاء؟ |
|---|---|---|---|
| 👤 **عميل** | طلب خدمة منك فعلاً | عروض ولاء · دعوة لتقييم · استرجاع الغائبين | نعم |
| 🎯 **ليد** | سجّل اهتمامه من حملة (تيك توك مثلاً) وما جرّبك | رسالة تعريفية + خصم أول طلب | لا — إلا لما يطلب |
| ❓ **غير مصنّف** | ما تدري | يظهر لك تنبيه لتصنيفه | لا |

- عند اختيار «🎯 ليد» يظهر حقل **اسم الحملة** (مثل `تيك توك أغسطس`) — يُحفظ مع الرقم فتعرف كل رقم من أي حملة جاك.
- **قائمة مخلوطة عندك أصلاً؟** الصق تصدير الحملة من تيك توك واختر «🎯 ليدز» — الأرقام الموجودة **تتعلّم** بدل ما تتجاهل (يظهر لك «صنّفنا موجوداً: X»)، ثم اضغط «👤 الباقي عملاء» ليُصنّف من تبقّى دفعة واحدة.
- **التصنيف يرتفع ولا ينزل:** غير مصنّف ← ليد ← عميل. لو رقم مصنّف عميلاً ولصقته ضمن دفعة ليدز يبقى عميلاً — ما نخسر معلومة أكيدة.
- **التقييم يرقّي تلقائياً:** أي ليد وصل منه تقييم يصير «🎯→👤 تحوّل» بدون أي خطوة منك، ويظهر عدّاد «ليد صار عميلاً» — هذا مقياس نجاح حملتك الحقيقي.
- زر التصنيف (👤 / 🎯 / ❓) بجانب كل رقم في القائمة يغيّره يدوياً بضغطة.

### 1.1.1) الفرز السريع — لقائمة مخلوطة

لما تكون الدفعة مخلوطة (بعضهم طلب الخدمة وبعضهم لا) فأزرار «الباقي عملاء / الباقي ليدز» ما تنفع، لأنها تعلّم الكل بنفس التصنيف. استخدم بطاقة **🗂 فرز سريع** — تظهر تلقائياً ما دام عندك أرقام بلا تصنيف:

- تعرض **رقماً واحداً في كل مرة** مع اسمه واسم حملته.
- زر **«💬 افتح محادثته وشوف»** يفتح محادثة واتساب معه (بدون رسالة) عشان تتذكر هل طلب منك خدمة ولا لا.
- زرّان كبيران: **«👤 طلب الخدمة»** أو **«🎯 ما طلب»** — وينتقل للتالي على طول.
- **«⏭ بعدين»** يتخطاه لهذي الجلسة، و**«🚫 رقم غلط · لا ترسل»** يوقفه نهائياً.
- **«↩️ تراجع»** يرجّع آخر رقم فرزته لو ضغطت غلط.
- شريط تقدم وعدّاد: «فرزت 12 من 163».

من وصل منه تقييم ما يدخل الفرز أصلاً — يُحسب عميلاً تلقائياً. والبطاقة تختفي وحدها لما تخلّص.

### 1.2) الإرسال حسب الفئة

في «🚀 الإرسال السريع» صف **«أرسل لـ»**: الكل · 👤 العملاء فقط · 🎯 الليدز فقط · ❓ غير المصنّف. اختيارك يحكم الدور والعدّادات وملفات البث (VCF) — فتقدر ترسل عرض ولاء لعملائك ورسالة تعريفية لليدز بدون ما تختلط. عند استهداف الليدز يذكّرك التنبيه باستخدام قالب **«🎯 ليد من الحملة»** (رسالة تعريف + خصم أول طلب، ما تخاطبهم كعملاء قدامى).

في تبويب **👥 العملاء** تظهر لهم خانة مستقلة «🎯 ليدز الحملات — ما جرّبونا بعد» بزر «💬 ادعُه يجرّب» — وما يُحسبون ضمن عملائك ولا يدخلون في التقارير. وملف التصدير (CSV) صار فيه عمودا **المصدر** و**الحملة**.

### 2) الرسالة

اختر قالباً جاهزاً أو اكتب رسالتك، واستخدم هذه الكلمات وتُستبدل تلقائياً لكل عميل:

| الكلمة | تتحول إلى |
|---|---|
| `{الاسم}` | اسم العميل (أو «عميلنا العزيز» إن كان الاسم ناقصاً) |
| `{الخصم}` | قيمة حقل الخصم |
| `{المدة}` | مدة الصلاحية بصياغة عربية سليمة (يومين / 5 أيام) |
| `{التحية}` | تحية حسب وقت الإرسال (صباحك خير / هلا وغلا / مساك الله بالخير) |
| `{الشركة}` · `{المندوب}` · `{واتساب}` | من متغير `BIZ` أعلى السكربت في `index.html` |

**«اسم العرض»** مهم: هو الذي يميّز الحملة. أي عميل استلم عرضاً بهذا الاسم لا يظهر لك مرة ثانية، حتى لو أضفت أرقاماً جديدة بكرة أو فتحت اللوحة من جهاز آخر. غيّر الاسم = حملة جديدة تبدأ من الصفر.

### 3) طريقتان للإرسال

- **الإرسال السريع (رسالة باسم كل عميل):** زر أخضر واحد يفتح واتساب والرسالة مكتوبة كاملة باسم العميل — تضغط «إرسال» في واتساب وترجع، واللوحة تكون انتقلت للعميل التالي. فيه «⏭ تخطي» و«🔕 لا ترسل له» (إيقاف دائم لهذا العميل)، وشريط تقدم يبيّن كم أُرسل وكم باقي.
- **قائمة البث (الأسرع للأعداد الكبيرة):** «📇 نزّل جهات الاتصال (VCF)» ينزّل ملفاً لكل 250 رقم — احفظه في جهات اتصال الجوال، ثم في **واتساب للأعمال → قائمة بث جديدة** اختر جهات الاتصال والصق نص الرسالة (زر «📋 انسخ نص الرسالة»). رسالة واحدة تصل الجميع دفعة واحدة، لكن بشرط واتساب: **لا تصل إلا لمن حفظ رقمك عنده**، وبدون تخصيص الاسم. لذلك: البث للقوائم الكبيرة، والإرسال السريع للعملاء المهمين.

### 4) الأرقام تظهر في تبويب «👥 العملاء» تلقائياً

كل رقم تضيفه في قائمة العروض ينضم مباشرة لتبويب العملاء:

- **إذا كان الرقم لعميل سبق وقيّمنا** يندمج مع سجله (ما يتكرر)، ويظهر بتقييماته وعدد خدماته كالمعتاد.
- **إذا كان رقماً جديداً ما وصلنا منه تقييم** يظهر في قسم جديد اسمه «📋 من القائمة — ما قيّمونا بعد» مع زر «💬 اطلب تقييمه» يفتح واتساب برسالة تدعوه للتقييم مع رابط النموذج ووعد بكوبون خصم.
- **لا يوجد رقم مخلوط:** بطاقات الأرقام أعلى التبويب منفصلة عمداً — «عملاء قيّمونا» و«عملاء متكررون» من التقييمات وحدها، و«أرقام في قائمة العروض» و«منها ما قيّمونا بعد» من قائمتك. ما فيه بطاقة تجمع الاثنين حتى لا يختلط عدد عملائك الحقيقيين بعدد الأرقام المضافة.
- **التقارير ما تتأثر:** التقييم العام، نسبة الترشيح، تحليل الأداء، أداء الفرق، التقرير الشهري وتقرير الشركة — كلها تُحسب من التقييمات فقط. إضافة ألف رقم للقائمة ما تغيّر فيها رقماً واحداً.
- **حماية من التكرار:** المطابقة تتم بالرقم بعد توحيد صيغته (`0501234567` = `+966501234567`)، وللتقييمات القديمة اللي ما فيها حقل جوال تتم المطابقة بالاسم — فما يظهر لك العميل نفسه مرتين.
- من أوقفت عنه الرسائل (🔕) يبقى محسوباً في أرقام القائمة لكن ما يظهر في قسم طلب التقييم.

### ملاحظات مهمة

- واتساب العادي/للأعمال ما يسمح بإرسال آلي كامل بدون تدخلك — أي أداة تعدك بذلك تعرّض رقمك للحظر. الطريقة الوحيدة الرسمية للإرسال الآلي هي **WhatsApp Cloud API** (تحتاج حساب Meta Business، وتوثيق الرقم، ورسائل قوالب معتمدة مسبقاً، ورسوم لكل محادثة). التبويب الحالي مبني ليعطيك أقصى سرعة ممكنة بدون هذي التعقيدات وبدون مخاطرة على رقمك.
- لا ترسل دفعة كبيرة جداً في وقت قصير من رقم جديد، وخلّ رسائلك مفيدة وواضحة، واحترم من يطلب إيقاف الرسائل (زر 🔕 يوقفه نهائياً ويستثنيه من كل الحملات وملفات جهات الاتصال).

## تحديث: تبويب «💰 الرواتب»

نافذة مستقلة لرواتب الموظفين — منفصلة تماماً عن التقييمات والعملاء، وما تدخل في أي تقرير منها.

**قبل الاستخدام:** أعد نسخ `firestore.rules` كاملاً والصقه في **Firestore → Rules → Publish** — أضيفت مجموعتان جديدتان (`staff` للموظفين و`payruns` لكشوف الشهور) مقفلتان على الأدمن وحده.

### الموظفون

- أول مرة تفتح التبويب يظهر زر **«⚡ جهّز فريقي»** يضيف مشرفاً براتب 2500 وثلاثة عمال براتب 1400 لكل واحد — ثم عدّل الأسماء بزر «✏️ تعديل».
- الإضافة اليدوية من الحقول تحت: الاسم · الراتب · المسمى · الجوال (اختياري، يُستخدم لإرسال كشف الراتب على واتساب).
- الحذف ما يمس كشوف الشهور السابقة — تبقى محفوظة كما هي.

### موعد الصرف

- يوم واحد موحّد للجميع (الافتراضي **30**)، يتغيّر من بطاقة «⚙️ موعد الصرف».
- **الشهور الأقصر:** لو الشهر أقل من يوم الصرف (فبراير مثلاً) يُصرف تلقائياً **آخر يوم في الشهر** بدل أن يُفوَّت. التبكير يوماً أو يومين لا إشكال فيه، أما تأجيله لأول الشهر التالي فيعني أن الشهر مرّ بلا راتب ويجتمع راتبان في شهر واحد.
- **الراتب شهري لا يومي:** مبلغه واحد سواء كان الشهر 28 أو 30 أو 31 يوماً — لا يُخصم منه لقصر الشهر ولا يُزاد لطوله. (الأجر اليومي = الشهري ÷ 30 يُستخدم فقط في الحسابات الجزئية كبدل أيام الانتقال.)
- شريط الحالة أعلى القائمة يعرض: باقي كم يوم · أو «اليوم موعد الصرف» · أو «تأخّر الصرف X يوم» بالأحمر · أو «مصروفة كاملة» بالأخضر.

### الصرف والخصومات

- زر **«صرف»** لكل موظف يسجّل المبلغ وتاريخ الصرف، وزر **«✅ صرف الكل»** يصرف المتبقي دفعة واحدة بعد تأكيد المبلغ.
- الضغط على موظف مصروف يتراجع عن الصرف (بتأكيد).
- زر **«± خصم/مكافأة»** يفتح محرّراً تحت الموظف فيه **خانتان منفصلتان**: «خصم أو سلفة» و«مكافأة أو بدل» — كل رقم يُكتب **موجباً في خانته**، بلا إشارة سالب، فتقدر تخصم وتكافئ في نفس الشهر. الصافي يتحدث وأنت تكتب قبل الحفظ، والراتب الأساسي ما يتغيّر.
- **حارس ضد الأرقام الغلط:** لو الخصم أكبر من مستحق الموظف يظهر تنبيه أحمر ويُرفض الحفظ — الصافي ما يصير بالسالب أبداً. لو السلفة أكبر من راتب شهر، قسّمها على شهور.
- التعديل يخص **هذا الشهر وحده**، والشهر الجاي يبدأ نظيفاً.
- زر **«💬»** يرسل للموظف كشف راتبه على واتساب: الأساسي، الخصم أو الإضافة وسببها، الصافي، وتاريخ الصرف.

### بدل أيام الانتقال

لما توحّد يوم الصرف، كل موظف كان يستلم قبل اليوم الجديد ينتظر أياماً زيادة مرة واحدة. بطاقة **«🔄 بدل أيام الانتقال»** تحسب له بدل تلك الأيام:

- خانة **«أيام»** جنب كل اسم هي عدد الأيام الزيادة، وتُحسب تلقائياً من موعده السابق (زر «⚡ جهّز فريقي» يضع: 20 للمشرف، 22 و27 و27 للعمال) — **وتقدر تعدّلها يدوياً** لأي رقم تبيه والمبلغ يتحدث فوراً. موعده السابق يبقى معروضاً تحت اسمه للمرجع، والصف المعدّل يدوياً يظهر عليه «معدّل يدوياً».
- **للرجوع للحساب التلقائي:** افرغ خانة الأيام لصف واحد، أو اضغط زر **«↺ رجّع الحساب التلقائي»** الذي يظهر ما دام أي صف معدّلاً يدوياً — يرجّع الجميع للفرق بين موعدهم السابق ويوم الصرف، ولا يمس مواعيدهم المحفوظة.
- الأجر اليومي = **الراتب ÷ 30** (المتبع في نظام العمل السعودي)، والبدل = الأجر اليومي × عدد أيام الفرق.
- زر **«✅ أضف البدل»** يضيفه لكشف الشهر المعروض فقط، ويتحول الزر بعدها إلى **«↩️ ألغِ البدل»**.
- البدل يُخزَّن في حقل مستقل، فما يمس السلف والخصومات ولا يتضاعف لو أُضيف مرتين، ويظهر مفصّلاً في صف الموظف وفي كشف الراتب على واتساب وفي ملف CSV.
- **البطاقة تخص شهر الانتقال وحده.** أول ما تضغط «أضف البدل» يُثبَّت ذلك الشهر (يُحفظ في `settings/payroll`)، فتختفي البطاقة من كل الشهور الأخرى — سابقة ولاحقة — وترجع تظهر لو تنقّلت لشهر الانتقال نفسه. وقبل التطبيق تظهر في الشهر الحالي فقط.
- الشهور التالية تبدأ بدون بدل — لأنه مستحق مرة واحدة عند الانتقال.

### من وين جاء الإجمالي

تحت بطاقات الأرقام يظهر سطر يفكّ الإجمالي متى ما وُجد خصم أو مكافأة أو بدل:

> الإجمالي = رواتب أساسية **6,700** − خصومات **300** + مكافآت **200** + بدل انتقال **1,486.66** = **8,086.66**

فإذا استغربت رقم الإجمالي، السطر يقول لك سببه مباشرة بدل ما تجمع الصفوف بيدك.

### الشهور والتصدير

- أزرار «◀ السابق» و«التالي ▶» تتنقل بين الشهور، وكل شهر كشف مستقل بحالته الخاصة (`payruns/YYYY-MM`).
- زر **«⬇️ كشف الشهر (CSV)»** يصدّر كشفاً كاملاً بالأساسي والخصومات والصافي وحالة الصرف والإجمالي.
- التواريخ في هذا التبويب **ميلادية** لأن موعد الصرف نفسه ميلادي (يوم 30).

## ملاحظات

- مفتاح `apiKey` الظاهر في `index.html` ليس سراً — هذا معرّف عام لمشروع Firebase، والحماية الفعلية تأتي من القواعد أعلاه ومن تسجيل الدخول.
- التقييمات القديمة المخزّنة قبل هذا التحديث لا تحتوي حقل رقم الجوال (`ph`) — هذا طبيعي وستظهر بدونه.
- إن أردت تشديداً إضافياً ضد الإرسال الآلي المتكرر، فعّل **App Check** من Firebase Console لاحقاً (خطوة اختيارية).
- مدة منع الإرسال المتكرر من نفس الجهاز حالياً **ساعة واحدة**، وتقدر تغيّرها من المتغير `SUBMIT_COOLDOWN_MS` في أعلى السكربت داخل `index.html`.
